<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nbsaas.boot.model.data.mapper.ModelMapper">
    <update id="executeSql">
        ${sql}
    </update>

    <select id="page" resultType="com.nbsaas.boot.rest.response.MapResponse">
        ${sql}
    </select>
    <select id="selectResponse" resultType="com.nbsaas.boot.rest.response.MapResponse">
        ${sql}
    </select>
</mapper>